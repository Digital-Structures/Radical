<UserControl x:Class="Radical.VariableControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Radical"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">

    <Border BorderThickness="0,0.3,0,0.3" BorderBrush="LightGray" Height="45" Width="380">


        <Grid Style="{x:Null}">
            <StackPanel Orientation="Horizontal" Style="{x:Null}">
                <TextBox Text="{Binding Name, Mode=TwoWay}" FontFamily="Roboto" FontSize="12" VerticalAlignment="Center" Margin="20,0,10,0" Width="50"></TextBox>
                <CheckBox Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked"  IsChecked="{Binding IsEnabled, Mode=TwoWay}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0"></CheckBox>
                <TextBox  PreviewTextInput="TextBox_PreviewTextInput" Text="{Binding Min, Mode=TwoWay, StringFormat=N2}"  VerticalAlignment="Center" FontSize="12" Margin="10,0" BorderBrush="{x:Null}" Width="25"></TextBox>
                <Slider Minimum="{Binding Min, Mode=TwoWay}" Maximum="{Binding Max, Mode=TwoWay}" Value="{Binding Value, Mode=TwoWay}"  AutoToolTipPlacement="BottomRight" AutoToolTipPrecision="1" FontSize="8" VerticalAlignment="Center" Foreground="LawnGreen" HorizontalAlignment="Center" Margin="10,0" Width="150" ></Slider>
                <TextBox  PreviewTextInput="TextBox_PreviewTextInput" Text="{Binding Max, Mode=TwoWay,StringFormat=N2}" VerticalAlignment="Center" FontSize="12" Margin="10,0" BorderBrush="{x:Null}" Width="25"></TextBox>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
