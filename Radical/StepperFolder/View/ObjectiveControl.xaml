<UserControl x:Class="Stepper.ObjectiveControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Stepper"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">

    <Grid Margin="50,10,0,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBox Name="ObjName" Text="{Binding Path=Name, Mode=TwoWay}" FontSize="18"
                     Grid.Column="0" Width="Auto" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0"
                     Visibility="Visible"
                     ></TextBox>

        <TextBlock Name="ValueText" Grid.Column="1" Text="{Binding Value, Mode=OneWay}" FontSize="18" Foreground="{DynamicResource SecondaryAccentBrush}"
                     Width="Auto" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0"
                     ></TextBlock>
    </Grid>
</UserControl>
