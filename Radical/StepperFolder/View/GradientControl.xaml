<UserControl x:Class="Stepper.GradientControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Stepper"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="50,10,0,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBox Name="VarName" Text="Variable" FontSize="18"
                     Grid.Column="0" Width="Auto" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0"
                     Visibility="Visible"
                     ></TextBox>

        <TextBlock Name="ValueText" Grid.Column="1" Text="{Binding Gradient, Mode=OneWay}" FontSize="18" Foreground="{DynamicResource SecondaryAccentBrush}"
                     Width="Auto" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0"
                     ></TextBlock>
    </Grid>
</UserControl>
