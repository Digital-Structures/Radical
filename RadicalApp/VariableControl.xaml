<UserControl x:Class="RadicalApp.VariableControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RadicalApp"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="370">

    <Grid Style="{x:Null}">
        <StackPanel Orientation="Horizontal" Style="{x:Null}">
            <TextBlock Text="V" FontFamily="Roboto" FontSize="12" VerticalAlignment="Center" Margin="10,0" Width="20"></TextBlock>
            <CheckBox Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked"  HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0"></CheckBox>
            <TextBox Text="0.0" VerticalAlignment="Center" FontSize="12" Margin="10,0" BorderBrush="{x:Null}"></TextBox>
            <Slider VerticalAlignment="Center" Foreground="LawnGreen" HorizontalAlignment="Center" Margin="10,0" Width="200" ></Slider>
            <TextBox Text="1.0" VerticalAlignment="Center" FontSize="12" Margin="10,0" BorderBrush="{x:Null}"></TextBox>
        </StackPanel>
    </Grid>
</UserControl>
